//! Нативный аудио-движок: захват через cpal (плюс pulse-маршрут на Linux),
//! синтез дрона, воркеры анализа. Wasm-аналог — `audio::wasm`.

// Весь предметный код лежит в `imp.rs`, а не здесь: до 2026-09-03 он был
// инлайн-модулем `mod imp { … }` прямо в этом файле — 1830 строк под лишним
// уровнем отступа. Имя `imp` сохранено, потому что оно уже написано в путях
// (`audio::mod` реэкспортирует `native::imp::AudioEngine`) и в комментариях,
// объясняющих, кто именно пишет `RecorderStatus`/`ReplayStatus`.
pub(super) mod imp;
